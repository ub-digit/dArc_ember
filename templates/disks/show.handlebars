<div class="container-fluid">
	<div class="page-head">
		<div class="row v-align">
			<div class="col-xs-9">
				<h1>
				    {{#if model.title}} {{model.title}} {{else}} {{translateString STRINGS.PAGES.DISK.TITLE}} {{/if}}
				</h1>
			</div>
		</div> <!-- end row --> 
	</div> <!-- end page-head --> 

	{{#if hasErrors}}
		<h4 class="alert alert-danger">{{translateString STRINGS.ERRORS.DISK.REMOVE_GENERAL}}</h4>
	{{/if}}	

	<div class="row">
		<div class="col-xs-12">
			<table class="table table-striped">
				<tr>
					<th>{{translateString STRINGS.FIELDS.ID}}:</th>
					<td>{{model.id}}</td>
				</tr>
				<tr>
					<th>{{translateString STRINGS.FIELDS.PID}}:</th>
					<td>{{model.pid}}</td>
				</tr>
				<tr>
					<th>{{translateString STRINGS.FIELDS.TITLE}}:</th>
					<td>{{model.title}}</td>
				</tr>
				<tr>
					<th>{{translateString STRINGS.FIELDS.ARCHIVES}}:</th>
					<td>
						{{#each model.archives }}{{#link-to 'archives.show' id}}{{title}} ({{id}}) {{/link-to}}{{/each}}
					</td>
				</tr>							
						
			</table>
		</div>
		<div class="col-xs-2">
			<button {{bind-attr class=":btn :btn-sm :btn-danger :btn-block"}} {{action 'deleteDisk'}}>{{translateString STRINGS.BUTTON.REMOVE}}</button>
		</div>
	</div><!-- end row --> 
</div> <!-- end container --> 